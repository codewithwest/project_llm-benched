# v0.3.1 — Requests Filter Refresh Fix

## Bug Fixes

- **Preserved Requests filters during polling** — selecting a path no longer resets the Requests tab to all requests when new traffic arrives
- **Prevented stale refresh results** — older polling responses can no longer overwrite a newer filtered result
- **Model name on request cards** — cards now show the captured model before opening request details

## Improvements

- Request filter state remains active across automatic refreshes
- Request cards keep endpoint and model context visible at a glance
